# Orchestrator smoke (offline)

- `sample-view-plan.md` — synthetic View Plan that passes `helpers/view_plan_schema.py`
- Not a live Archi MCP transcript

Live end-to-end orchestrator evidence against a real model is tracked as EVID-01 (roadmap phase 5).
