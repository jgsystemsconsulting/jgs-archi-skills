---
phase: 01-foundations-and-mcp-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mcp/archi-bridge-inventory.json
  - docs/MCP.md
  - helpers/validate_skill_mcp_refs.py
  - helpers/__init__.py
  - tests/test_validate_skill_mcp_refs.py
  - skills/.gitkeep
  - install.py
  - README.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
user_setup: []

must_haves:
  truths:
    - Install script copies or links in-repo skills into ~/.zcode/skills/
    - Structural validator exits non-zero when a skill cites an unknown MCP tool or resource
    - Docs state default MCP endpoint and consume-only (no plugin fork) policy
  artifacts:
    - path: docs/mcp/archi-bridge-inventory.json
      provides: Canonical 69-tool / 14-resource name list for offline validation
    - path: helpers/validate_skill_mcp_refs.py
      provides: Stdlib-only CLI that scans skill markdown for MCP refs
    - path: install.py
      provides: Install path from repo skills/ to ~/.zcode/skills/
    - path: docs/MCP.md
      provides: Endpoint + consume-only contract for contributors
  key_links:
    - from: helpers/validate_skill_mcp_refs.py
      to: docs/mcp/archi-bridge-inventory.json
      via: loads tools[] and resources[] as allowlists
    - from: install.py
      to: skills/
      via: discovers skill dirs with SKILL.md and installs them
---

<objective>
Stand up repo foundations for the Archi skill suite: MCP contract inventory, structural skill↔MCP validator, install script, and contributor docs (FOUND-01..03).

Purpose: Unblock later orchestrator/specialist work with a hard gate against invented MCP names and a real install path.
Output: inventory JSON, validator + tests, install.py, docs/MCP.md, README, empty skills tree placeholder.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@docs/mcp/archi-bridge-inventory.json
@VISION.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze MCP inventory snapshot</name>
  <files>docs/mcp/archi-bridge-inventory.json, docs/MCP.md</files>
  <read_first>docs/mcp/archi-bridge-inventory.json, VISION.md, AGENTS.md</read_first>
  <action>
Ensure `docs/mcp/archi-bridge-inventory.json` lists exactly 69 tools and 14 resources matching jgs-archi-mcp README Available Tools bullets and ResourceHandler URIs. Fields required: `default_endpoint` = `http://127.0.0.1:18090/mcp`, `tools` (sorted unique hyphenated names), `resources` (full `archimate://...` URIs), `policy.consume_only` true, `policy.no_plugin_modification` true, `source` citing jgs-archi-mcp.

Write `docs/MCP.md` with:
- Default endpoint `http://127.0.0.1:18090/mcp`
- Statement that skills consume MCP tools/resources only and must never modify jgs-archi-mcp
- Statement that ArchiMate language reference lives in MCP resources (do not copy tables into skills)
- Pointer to `docs/mcp/archi-bridge-inventory.json` as offline allowlist
- Note live discovery preferred when Archi is running; inventory is the offline gate
  </action>
  <verify>python -c "import json; d=json.load(open('docs/mcp/archi-bridge-inventory.json')); assert len(d['tools'])==69 and len(d['resources'])==14"</verify>
  <acceptance_criteria>
    - docs/mcp/archi-bridge-inventory.json has tool_count 69 and resource_count 14
    - docs/MCP.md contains exact string `http://127.0.0.1:18090/mcp`
    - docs/MCP.md contains consume-only / no plugin modification guidance
  </acceptance_criteria>
  <done>Inventory and MCP.md committed and counts assert.</done>
</task>

<task type="auto">
  <name>Task 2: Structural skill MCP ref validator</name>
  <files>helpers/validate_skill_mcp_refs.py, helpers/__init__.py, tests/test_validate_skill_mcp_refs.py</files>
  <read_first>docs/mcp/archi-bridge-inventory.json</read_first>
  <action>
Implement Python 3.10+ stdlib-only `helpers/validate_skill_mcp_refs.py`:
- CLI: `python helpers/validate_skill_mcp_refs.py [--inventory PATH] [--skills-root PATH]`
- Defaults: inventory `docs/mcp/archi-bridge-inventory.json`, skills-root `skills/`
- Scan all `**/SKILL.md` under skills-root (if none, exit 0 with message "no skills")
- Extract candidate refs: backtick tokens matching `^[a-z][a-z0-9-]+$` with a hyphen (tool-like) and URIs matching `archimate://[a-z0-9/.-]+`
- Also match bare tool names after phrases `tool ` / `mcp tool ` case-insensitive when followed by hyphenated name
- Compare tools against inventory tools set; resources against inventory resources set (accept URI with or without trailing path variants only if exact match)
- Exit code 1 and print each unknown ref with file:line if any unknown; exit 0 if clean
- Do NOT network; inventory file only

Add `tests/test_validate_skill_mcp_refs.py` using unittest:
- temp skill with known tool `get-model-info` → pass
- temp skill with fake tool `create-unicorn-element` → fail exit 1
- temp skill with resource `archimate://reference/archimate-layers` → pass
- temp skill with fake resource `archimate://reference/nope` → fail
  </action>
  <verify>python -m unittest tests.test_validate_skill_mcp_refs -v</verify>
  <acceptance_criteria>
    - helpers/validate_skill_mcp_refs.py exists and imports only stdlib
    - unittest module passes all four cases
    - Running validator on empty skills/ exits 0
  </acceptance_criteria>
  <done>Validator + tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Install script and skills tree</name>
  <files>install.py, skills/.gitkeep, README.md</files>
  <read_first>VISION.md, AGENTS.md, docs/MCP.md</read_first>
  <action>
Create `skills/.gitkeep` so the skills package root exists.

Implement stdlib-only `install.py`:
- Discover immediate subdirs of `skills/` that contain `SKILL.md`
- Install each to `~/.zcode/skills/<name>/` (expand user home)
- Default mode: copytree (replace existing dest after rmtree of dest only if it looks like a prior install — safety: only delete dest if dest/SKILL.md exists)
- Flag `--link` optional: on Unix try symlink; on Windows copy (document limitation) or use directory junction if easy without third-party deps; if link unsupported, fall back to copy and print notice
- Print each installed path; exit 0 when zero skills with message
- Never touch jgs-archi-mcp paths

Write root `README.md` covering: what the repo is, prerequisites (Archi + JGS Archi Bridge MCP), `python install.py`, `python helpers/validate_skill_mcp_refs.py`, link to docs/MCP.md and VISION.md, note Phase 1 foundations only (orchestrator comes Phase 2).
  </action>
  <verify>python install.py &amp;&amp; python helpers/validate_skill_mcp_refs.py</verify>
  <acceptance_criteria>
    - install.py runs with exit 0 on empty skills tree
    - README.md mentions install.py and validate_skill_mcp_refs.py
    - README.md mentions http://127.0.0.1:18090/mcp or points to docs/MCP.md
  </acceptance_criteria>
  <done>Install path and README usable.</done>
</task>

</tasks>

<claim_verification>
Load-bearing claims verified against real sources before planning:

1. Tool count 69 and resource count 14 — verified from jgs-archi-mcp README.md "Available Tools" bullet extraction (69 names) and ResourceHandler.java RESOURCE_DEFINITIONS (14 archimate:// URIs). Command used during planning: Python parse of README bullets + ResourceHandler map keys.
2. Default endpoint `http://127.0.0.1:18090/mcp` — stated in owner VISION.md and AGENTS.md.
3. No application source in this repo yet — `ls` of jgs-archi-skills shows only planning + VISION/AGENTS before this phase.
4. Python stdlib-only policy — VISION.md implementation direction.
5. Inventory file pre-seeded at docs/mcp/archi-bridge-inventory.json during plan host bootstrap from the same sources; executor must re-verify counts and may refresh from sibling checkout `../jgs-archi-mcp` if present without modifying that repo.
</claim_verification>

<success_criteria>
Phase 1 success criteria from ROADMAP:
1. Install script places suite skills under ~/.zcode/skills/ from this repo
2. Structural validation helper fails clearly on unknown MCP tool/resource names
3. Docs state MCP default endpoint and consume-only rule
</success_criteria>

<verification>
Run after all tasks:
- python -c "import json; d=json.load(open('docs/mcp/archi-bridge-inventory.json')); assert d['tool_count']==69 and d['resource_count']==14"
- python -m unittest tests.test_validate_skill_mcp_refs -v
- python helpers/validate_skill_mcp_refs.py
- python install.py
- rg -n "18090/mcp|consume" docs/MCP.md README.md
</verification>
