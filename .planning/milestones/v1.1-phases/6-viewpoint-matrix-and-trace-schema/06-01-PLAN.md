---
phase: 06-viewpoint-matrix-and-trace-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpers/viewpoint_selection_matrix.py
  - helpers/viewpoint_trace_schema.py
  - tests/test_viewpoint_selection_matrix.py
  - tests/test_viewpoint_trace_schema.py
autonomous: true
requirements: [VSEL-01, VSEL-02, VSEL-03, VSEL-05]
must_haves:
  truths:
    - "matrix helper ranks candidates from intent axes JSON"
    - "malformed intent exits non-zero"
    - "trace schema validates required columns"
    - "fixtures hold keys/axes only (no element catalogs)"
  artifacts:
    - helpers/viewpoint_selection_matrix.py
    - helpers/viewpoint_trace_schema.py
  key_links: []
---
<objective>
Ship stdlib viewpoint selection matrix and Trace Table schema validators for OBJ-2 / VSEL-01..03, VSEL-05.
</objective>
<tasks>
<task type="auto">
  <name>Implement viewpoint_selection_matrix helper</name>
  <files>helpers/viewpoint_selection_matrix.py, tests/test_viewpoint_selection_matrix.py</files>
  <action>
  Create helpers/viewpoint_selection_matrix.py (Python 3.10+, stdlib only):
  - Built-in CANDIDATES list: each entry has key (stable id), label (display name), purposes (set), abstractions (set: overview|detail|mixed), concern_tags (set), stakeholder_tags (set). Keys only — no element-type lists, no relationship catalogs.
  - Include at least: motivation, business-process, application-structure, application-cooperation, technology, physical, implementation-migration, layered, organisation-capability (or similar ArchiMate-common viewpoint keys as labels; definitions remain on MCP).
  - CLI: accept path to JSON intent file OR stdin. Intent schema: { "stakeholders": [str], "concerns": [str], "purpose": str, "abstraction": str }. purpose in {design, decide, inform} or free text mapped loosely; abstraction in {overview, detail, mixed}.
  - Scoring: +2 exact purpose match, +2 abstraction match, +1 per overlapping concern_tag/stakeholder_tag (casefold token match). Sort by score desc, key asc. Emit JSON: { "ranked": [{"key","label","score","matched"}], "threshold": number, "standard_fit": bool } where standard_fit true if top score >= threshold (default 3).
  - Malformed/missing required fields → stderr message, exit 1. Missing file → exit 2.
  - --list prints candidate keys as JSON (no scoring).
  Tests: ranking order stable for a sample multi-stakeholder intent; empty/malformed fails; --list returns keys; no forbidden catalog words like "Assignment relationship" in module source.
  </action>
  <verify>python -m pytest tests/test_viewpoint_selection_matrix.py -q</verify>
  <done>matrix helper green with fail paths</done>
</task>
<task type="auto">
  <name>Implement viewpoint_trace_schema helper</name>
  <files>helpers/viewpoint_trace_schema.py, tests/test_viewpoint_trace_schema.py</files>
  <action>
  Create helpers/viewpoint_trace_schema.py mirroring view_plan_schema style:
  - Required H2 sections: "Viewpoint Trace Table", "Organisation-Specific Proposals", "Rejected Alternatives"
  - Required table header columns (order flexible, all must appear in header row): Viewpoint, Stakeholder, Concern, Purpose, Abstraction, Standard?, Justification
  - CLI path or stdin; exit 0 ok, exit 1 missing pieces, exit 2 file not found.
  Tests: valid fixture passes; missing H2 fails; missing column fails.
  </action>
  <verify>python -m pytest tests/test_viewpoint_trace_schema.py -q</verify>
  <done>trace schema helper green</done>
</task>
</tasks>
<claim_verification>
After execute: both helpers importable; pytest modules pass; `python helpers/viewpoint_selection_matrix.py --list` exits 0; sample intent file scores without metamodel copy.
</claim_verification>
