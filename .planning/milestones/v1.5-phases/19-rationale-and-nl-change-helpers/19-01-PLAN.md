---
phase: 19-rationale-and-nl-change-helpers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpers/rationale_schema.py
  - helpers/completion_summary_schema.py
  - helpers/nl_change_impact.py
  - tests/test_rationale_schema.py
  - tests/test_completion_summary_schema.py
  - tests/test_nl_change_impact.py
autonomous: true
requirements: [RAT-01, RAT-02, RAT-03, RAT-04]
---
<!-- Copyright (c) 2026 JG Systems Consulting Ltd. See LICENSE. -->
<!-- SPDX-License-Identifier: LicenseRef-JGSC-Proprietary -->

<objective>Deterministic offline helpers for deep rationale validation, completion-summary structure, and NL-change impact planning (OBJ-6 depth).</objective>
<tasks>
<task type="auto">
  <name>Implement rationale depth, completion-summary, NL-change helpers + tests</name>
  <files>helpers/rationale_schema.py, helpers/completion_summary_schema.py, helpers/nl_change_impact.py, tests/test_rationale_schema.py, tests/test_completion_summary_schema.py, tests/test_nl_change_impact.py</files>
  <action>
  Stdlib-only. Deepen OBJ-6 offline helpers beyond thin v1.0 heading check. Do not call MCP. Do not mutate models.

  1. Extend `helpers/rationale_schema.py` (keep thin API):
     - Keep `REQUIRED` section list and `missing_headings(text)` (backward compatible).
     - Add section body extraction; empty body (whitespace-only) is a finding.
     - Add `validate_rationale(text) -> list[finding]` with check_ids: missing_section, empty_section; optional order_warning (non-fatal / warn severity).
     - Multi-view: accept a directory of `*.md` or a bundle JSON `{views:[{id,name,path|text}]}`; report per-view findings (`view_id` on each finding).
     - CLI: `python helpers/rationale_schema.py PATH` (file) or `--bundle DIR|JSON` ; exit 0 if no error findings; `--json` for machine output.
     - Existing single-file thin CLI behavior: still exit 1 on missing headings (tests must stay green).

  2. Add `helpers/completion_summary_schema.py`:
     - Required blocks/fields (H2 or labeled bullets): Views Touched, Decisions, Open Questions, Confirmation Status, Specialists Run.
     - `validate_completion_summary(text) -> findings`; CLI path + `--json`; exit 0/1.

  3. Add `helpers/nl_change_impact.py`:
     - Input: change note text + inventory JSON `{views:[{id,name,keywords?}], elements:[{id,name,shared?:bool,view_ids?:[...]}]}`.
     - Deterministic match: keyword / name token overlap between note and view names/keywords (casefold); always include views explicitly listed by id/name in the note.
     - Output impact plan: `{affected_views, regenerate_scope, must_reuse_element_ids, exclusions, notes}` — never mutates.
     - CLI: `python helpers/nl_change_impact.py note.txt inventory.json [--json]`.

  4. Unit tests covering pass/fail for each helper; multi-view bundle; empty section; incomplete summary; shared-ID preservation in impact plan.
  </action>
  <verify>python -m unittest discover -s tests -q</verify>
  <done>Helpers + tests green; thin rationale heading API still works; stdlib only</done>
</task>
</tasks>
<claim_verification>RAT-01..04 covered by rationale_schema depth, completion_summary_schema, nl_change_impact + unit tests.</claim_verification>
