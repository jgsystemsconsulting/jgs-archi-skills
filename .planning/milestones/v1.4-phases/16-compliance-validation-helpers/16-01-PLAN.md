---
phase: 16-compliance-validation-helpers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpers/compliance_validate.py
  - helpers/fixtures/compliance_allowlist.json
  - tests/test_compliance_validate.py
  - helpers/compliance_checklist.py
  - tests/test_compliance_checklist.py
autonomous: true
requirements: [COMP-03, COMP-04, COMP-05, COMP-06, COMP-07]
---
<!-- Copyright (c) 2026 JG Systems Consulting Ltd. See LICENSE. -->
<!-- SPDX-License-Identifier: LicenseRef-JGSC-Proprietary -->

<objective>Deterministic offline compliance validator over model-slice snapshots with explain-and-propose findings and unit tests.</objective>
<tasks>
<task type="auto">
  <name>Implement compliance validator + fixture + tests</name>
  <files>helpers/compliance_validate.py, helpers/fixtures/compliance_allowlist.json, tests/test_compliance_validate.py, helpers/compliance_checklist.py, tests/test_compliance_checklist.py</files>
  <action>
  Stdlib-only. Deepen OBJ-5 offline validation beyond the thin boolean checklist:

  1. Add `helpers/fixtures/compliance_allowlist.json` — minimal captured fixture (not a full ArchiMate catalog dump, NG-4) with:
     - known element types (subset covering common layers)
     - permitted relationship types
     - permitted (source_type, relationship_type, target_type) patterns
     - optional abstraction_level map (type → level id) for consistency checks
  2. Add `helpers/compliance_validate.py`:
     - Input: model-slice snapshot JSON `{elements:[{id,name,type,abstraction?}], relationships:[{id,type,source,target}], view_usages?:[{view_id,element_id,name?}]}` plus path to allowlist fixture (default beside module).
     - Checks: element_type_known, relationship_type_permitted, relationship_endpoints_valid, abstraction_level_consistent, cross_view_naming_consistent (reuse/mirror naming_convention normalize + conflicts when view_usages present).
     - Output: list of findings `{check_id, object_refs, problem, proposed_alternative}` — never mutates model; never silent-applies.
     - CLI: `python helpers/compliance_validate.py slice.json [--allowlist path] [--json]` exit 0 if no findings, 1 if findings.
  3. Keep `helpers/compliance_checklist.py` thin gate working (COMP-12). Optionally document that deep validation is `compliance_validate`; do not break existing checklist callers/tests.
  4. Unit tests `tests/test_compliance_validate.py`: pass slice; unknown element type; illegal relationship type; illegal endpoints; abstraction mismatch; naming conflict across views; every finding has problem + proposed_alternative.
  </action>
  <verify>python -m unittest discover -s tests -q</verify>
  <done>Validator + fixture + tests green; checklist still green; stdlib only</done>
</task>
</tasks>
<claim_verification>COMP-03..07 covered by compliance_validate + fixture + tests; checklist retained.</claim_verification>
